extern crate cirru_edn;

// use serde::{Deserialize, Deserializer, Serialize, Serializer};

// use cirru_edn::{Edn, EdnVisitor};

fn main() -> Result<(), String> {
  // let mut edn = cirru_edn::parse("[] a b c")?;

  // Edn::deserialize(edn);

  println!("TODO");
  Ok(())
}
